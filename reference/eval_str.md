# Helper function for add_group()

Helper function for add_group()

## Usage

``` r
eval_str(str, envir = parent.frame())
```

## Arguments

- str:

  A string of R code to evaluate.

- envir:

  An environment to evaluate `str` in.

## Value

Result of evaluating `str`
